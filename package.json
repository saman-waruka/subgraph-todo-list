{
  "name": "samansubgraph",
  "license": "UNLICENSED",
  "version": "0.0.40",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ samansubgraph",
    "create-local": "graph create --node http://localhost:8020/ samansubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ samansubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 samansubgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
